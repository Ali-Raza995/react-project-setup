
export enum REQUEST_STATUS {
    IDLE = "idle",
    PENDING = "pending",
    SUCCEEDED = "succeeded",
    FAILED = "failed",
    ERROR = "error"
  }
export enum RESPONSE_STATUS {
  FULFILLED= "fulfilled",
  REJECTED="rejected",
  PENDING="pending"
}

  